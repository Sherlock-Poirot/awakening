server:
  port: 8190
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://121.199.19.55:3306/awakening?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: stone
    password: Stn_123456
  application:
    name: company
eureka:
  client:
    fetch-registry: true  #获取注册实例列表
    register-with-eureka: true #注册到Eureka的注册中心
    service-url:
      defaultZone: http://localhost:8091/eureka #配置注册中心地址